
@if (Show)
{
    <div class="modal show d-block" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-@Size modal-dialog-scrollable @(Centered ? "modal-dialog-centered" : "")" role="document">
            <div class="modal-content shadow">
                @if (!HideTopAndBottom)
                {
                    <div class="py-1 modal-header">
                        <div class="col text-right p-0">
                            <button class="btn btn-outline-danger" disabled="@IsOperating" @onclick="Close">×</button>
                        </div>
                    </div>
                }
                <div class="modal-body">
                    @ChildContent
                </div>
                @if (!HideTopAndBottom)
                {
                    <div class="py-1 modal-footer">
                        <button class="btn btn-secondary" disabled="@IsOperating" @onclick="Close">Close</button>
                        @if (Save.HasDelegate)
                        {
                            <button class="btn btn-primary" disabled="@IsOperating" @onclick="OnSave">
                                @if (IsOperating)
                                {
                                    <span class="spinner-border spinner-border-sm"></span>
                                }
                                Save
                            </button>
                        }
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="modal-backdrop show"></div>
}
